/**
 * @module shared
 *
 * Shared layer — reusable, domain-agnostic building blocks.
 *
 * Rules:
 *  - NEVER import from `@shell` or `@modules`.
 *  - Only generic, stateless, presentation-oriented exports belong here.
 */
export * from './components';
